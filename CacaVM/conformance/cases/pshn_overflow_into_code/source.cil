; One PSHN with a large count must trip the same "own code" protection a
; long run of individual PSHes would (stack_overflow_into_code covers that
; case) — proving PSHN's per-byte check isn't skipped just because it's
; one instruction doing the work of many.
MOV AL, 0x42
PSHN 0x32
KEI 0x02
